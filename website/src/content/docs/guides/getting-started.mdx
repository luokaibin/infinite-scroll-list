---
title: 快速开始
description: 安装并使用 infinite-scroll-list 组件
---
import { Tabs, TabItem } from '@astrojs/starlight/components';

## 安装

<Tabs>
  <TabItem label="npm（工程化项目，推荐）">
    ```sh
    pnpm add @wc-lib/infinite-scroll-list
    ```
  </TabItem>
  <TabItem label="CDN（非工程化页面）">
    ```html
    <script src="https://unpkg.com/@wc-lib/infinite-scroll-list"></script>
    ```
  </TabItem>
</Tabs>

## 注册

npm 方式在应用入口导入一次即可完成注册：

```ts
// main.ts
import '@wc-lib/infinite-scroll-list';
```

CDN 方式加载脚本即完成注册，无需额外代码。

## 使用

```html
<infinite-scroll-list
  id="scrollView"
  has-next-page="true"
  on-end-reached-threshold="100"
>
  <div id="list">
    <!-- 列表内容 -->
  </div>

  <div slot="loading">正在加载更多数据...</div>
  <div slot="no-data">已经到底啦</div>
</infinite-scroll-list>

<script>
  const scrollView = document.getElementById('scrollView');
  scrollView.addEventListener('end-reached', async () => {
    const more = await fetchMoreData();
    renderList(more);
    if (!hasMore) {
      scrollView.setAttribute('has-next-page', 'false');
    }
  });
</script>
```

## 下一步

- 到 [组件文档](../components/infinite-scroll-list/) 在线编辑体验完整演示
- 工程化接入细节（ESM 产物、SSR、时序）见 [接入方式](./integration/) 与 [加载时序与 SSR](./ssr-and-timing/)
